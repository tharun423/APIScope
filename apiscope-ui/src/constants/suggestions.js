/**
 * Default suggestion chips shown on the AI Chat empty state.
 *
 * These are intentionally generic — override them by replacing this file or by
 * fetching suggestions from a custom backend endpoint if you need app-specific prompts.
 */
export const SUGGESTIONS = [
  'List all available API endpoints',
  'Show me how to make a POST request with the API',
  'What endpoints accept a request body?',
  'Generate Java code to call the first GET endpoint',
  'Which endpoints require path parameters?',
  'Explain what the DELETE endpoints do',
]
